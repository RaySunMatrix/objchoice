#!/usr/bin/perl

use strict;
use warnings;
use base;
use Carp;

sub private_pop_link {
    my $pop_link = shift;
    my $link = ".";
    my $ignore = "adverts";
}

sub public_pop_link {
    my $pop_link = shift;
    my $link = ".";
    my $ignore = "adverts";
}
